import 'package:flutter/material.dart';

class Statistical extends StatelessWidget {
  const Statistical({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Statistical");
  }
}
